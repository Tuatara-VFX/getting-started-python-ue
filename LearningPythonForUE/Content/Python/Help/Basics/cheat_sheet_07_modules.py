# Import the module named "math"
import math

# Use the module's function to calculate the square root of 144
result = math.sqrt(144)
print(result) # Output: 12.0

# Import only the "factorial" function from the "math" module
from math import factorial

# Use the imported function to calculate the factorial of 5
result = factorial(5)
print(result) # Output: 120
